from sys import argv; from os.path import exists; indata = open(argv[1]).read(); out_file = open(argv[2], 'w').write(indata)

# This is the most simplified I could make it, there is apparently a way to
# have a copying script be 1 line

 # nvm made it into one line
